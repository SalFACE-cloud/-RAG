QDRANT_URL=http://127.0.0.1:6333
MEILI_URL=http://127.0.0.1:7700
MEILI_MASTER_KEY=edu_meili_dev_2026
REDIS_HOST=localhost
REDIS_PORT=6379

# PostgreSQL（docker compose postgres 服务）
POSTGRES_DB=edu_kb
POSTGRES_USER=edu_admin
POSTGRES_PASSWORD=edu_dev_2026
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# MinIO 对象存储（开发环境）
MINIO_USER=edu_minio
MINIO_PASSWORD=edu_minio_2026
MINIO_ENDPOINT=http://127.0.0.1:9000

# Phase 2 流水线：true=入队由 Worker 消费；false=同步处理
PIPELINE_USE_RQ=false
RQ_JOB_TIMEOUT=600
RQ_MAX_RETRIES=3

VAULT_DIR=vault
EMBEDDING_MODEL=BAAI/bge-m3
RERANKER_MODEL=BAAI/bge-reranker-v2-m3
USE_FP16=true
USE_RERANK=false

LLM_API_BASE=
LLM_API_KEY=
LLM_MODEL=deepseek-chat

JWT_SECRET_KEY=change_me_in_production
JWT_EXPIRE_HOURS=24
AUTH_ENABLED=false
RATE_LIMIT_PER_MINUTE=60
AUDIO_SEED_PATH=data/audio_seed.json

NEO4J_URI=bolt://127.0.0.1:7687
NEO4J_PASSWORD=edu_neo4j_2026
GRAPH_ENABLED=true
