# Obsidian 配置说明

首次使用：

1. Obsidian → **Open folder as vault** → 选择本项目的 `vault/` 目录
2. 可将 `configs/obsidian/app.json` 复制为 `vault/.obsidian/app.json` 作为起点
3. 社区插件建议：Dataview、Obsidian Git、Excalidraw、Tag Wrangler

插件启用后 Obsidian 会自动维护 `vault/.obsidian/` 目录（个人/团队可选是否提交 git）。
