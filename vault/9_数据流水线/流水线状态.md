---
subject: MATH-S
type: summary
---

# 数据流水线状态

- **Workflow**：`Edu Knowledge RAG Pipeline`（`.github/workflows/knowledge-pipeline.yml`）
- **触发路径**：`vault/2_教材库/**`、`3_教辅资料/**`、`4_题库与试卷/**` 等（不含 `0_项目文档/`）
- **Pipeline B 日志**：`vault/9_数据流水线/logs/`（`chunks_manifest.json`、`pipeline_result.json`）
- **本地验收**：`python scripts/verify_pipeline_b.py`（mock）或 `python scripts/verify_phase2.py`（Pipeline A）
- 最后更新：2026-07-17

## Pipeline B 步骤（CI 与本地分步）

```
metadata_validator → file_tracker → chunker → embedder → meili_indexer
```

全量重建 vault 后：

```powershell
python services/pipeline/file_tracker.py --scan-mode full --vault-path ./vault
python services/indexer/chunker.py --vault-path ./vault
python services/indexer/embedder.py --vault-path ./vault
python services/indexer/meili_indexer.py
```
